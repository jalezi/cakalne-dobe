// day @mitar has started to collect data for the first time
export const FIRST_DAY = new Date(Date.UTC(2024, 3, 7));

const _maxDuration = 30;
export const MAX_CHUNK_SIZE = 50;
export const EXPECTED_NUMBER_OF_JOBS = 1;
